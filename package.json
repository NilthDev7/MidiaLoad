{
  "name": "midiaload-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && node -e \"require('fs').cpSync('frontend/.next', '.next', {recursive: true}); require('fs').cpSync('frontend/public', 'public', {recursive: true})\"",
    "start": "cd frontend && npm start",
    "dev": "cd frontend && npm run dev"
  }
}
